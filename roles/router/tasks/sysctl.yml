---
# Sysctl performance tuning tasks for OpenWrt
- name: Deploy sysctl performance config
  ansible.builtin.template:
    src: sysctl.conf.j2
    dest: /etc/sysctl.conf
    owner: root
    group: root
    mode: '0644'
  when: router_sysctl_enabled
  notify: Apply sysctl settings
